# TODO-CONTENT｜オーナー確認が必要な情報

公式Instagram（@lifesupport_kumagaya）の公開情報だけでサイトを制作しています。
以下は **Instagramで確認できなかったため掲載していない／断定を避けている** 項目です。
確定次第 `src/data/site.ts` を更新すると、フッター・JSON-LD・各ページに自動で反映されます。

## 1. 最優先（問い合わせ導線・NAP）

| 項目 | 現在の扱い | 確認後の反映先 |
| --- | --- | --- |
| 電話番号 | 未掲載（Instagramに「電話またはDM」とあるが番号は確認できず） | `site.tel` / `site.telLink` → フッター・CTA・JSON-LD `telephone` に自動反映 |
| 公式LINE | 未掲載 | `site.line` → CTA に自動で追加 |
| メールアドレス | 未掲載 | `site.email` |
| 所在地（住所） | 未掲載。JSON-LD は「埼玉県熊谷市」までの粗い住所 | `site.address` → フッター・JSON-LD `streetAddress` |
| 受付時間・営業日 | 未掲載 | `site.hours` → フッター・JSON-LD `openingHours` |
| Googleビジネスプロフィール | 有無が不明 | あれば名称・住所・電話の表記をサイトと完全一致させる |

## 2. サービス内容

| 項目 | 現在の扱い | 備考 |
| --- | --- | --- |
| 料金（利用料・配達料・最低利用金額など） | 「ご相談時にご案内」とのみ記載。料金セクションは未作成 | 確定後 `/shopping-support` `/flow` に料金セクションを追加 |
| 支払い方法（現金・立替精算・キャッシュレス等） | 「ご相談時にご案内」 | FAQ「買い物代と利用料金はどのように支払いますか？」を具体化 |
| 当日依頼の可否・受付締切 | 「ご相談ください」 | FAQ を具体化 |
| 定期利用の条件 | 「ご相談ください」 | FAQ を具体化 |
| お届け方法（手渡し・置き配・不在時対応） | `/flow` では一般的な表現に留めている | 確認後 `src/data/content.ts` の `FLOW` を具体化 |
| 対応エリアの扱い | Instagram記載の「熊谷市・深谷市・行田市・東松山市」を同列で掲載（熊谷市を中心エリアとして表記） | 市内の一部地域のみ等の制限があれば `site.areas` と `/area/kumagaya` を修正 |
| ドラッグストアの「お薬」 | Instagram投稿に「お薬」の記載があったが、処方薬の受け取りと誤解されないよう **市販薬は掲載していない**（マスク・衛生用品・サプリメントのみ） | 市販薬の購入代行を行う場合は `ITEMS` に追記 |
| サービス開始日 | 「2026年9月スタート予定」として各所に表示 | 開始後 `site.launch.status` を `"open"` に変更すると表示が消える |

## 3. 運営者・ブランド

| 項目 | 現在の扱い | 備考 |
| --- | --- | --- |
| 表示名の表記 | サイトは「ライフサポート熊谷」で統一 | Instagramのbio冒頭が「ライ**ブ**サポート熊谷」と読める箇所があった。誤記であればInstagram側の修正を推奨（検索一致のため） |
| 代表者名の表記 | 「齊藤 匠」（Instagram記載の「齊藤匠」を採用） | 漢字表記（齊/斉/齋）・読み仮名の確認 |
| 代表の経歴・資格 | 「福祉・介護の現場経験から生まれた」のみ。資格名は掲載していない | 介護福祉士等の資格があれば About・著者情報に追記可。**確認できるまで「有資格者」「監修」表記は禁止** |
| 法人格・屋号 | 不明（個人事業／法人） | 特定商取引法に基づく表記が必要な場合は `/legal` ページを追加 |
| ロゴ | Instagramにロゴ画像が確認できず、**若葉＋買い物袋のロゴマークを仮作成** | 正式ロゴがあれば `src/components/ui/icons.tsx` の `LogoMark` と `scripts/make-assets.ts` を差し替え |
| ブランドカラー | Instagramの雰囲気に合わせ黄緑 `#8cc63f` を採用 | 正式なカラーコードがあれば `globals.css` の `--color-leaf` 等を調整 |

## 4. 写真

`public` に写真素材がなかったため、**すべてSVGイラストで構成**しています。

- ヒーロー（買い物袋のイラスト）→ 実際の写真（代表者・お届けの様子・熊谷の風景など）に差し替え推奨
- OGP画像 `public/og.jpg` → 写真版に差し替え可（`scripts/make-assets.ts` で再生成）
- About ページ → 代表者の写真があると E-E-A-T 上有利
- ブログのアイキャッチ → 現在は全記事 `/og.jpg`。写真が揃ったら `scripts/generate-daily-post.ts` に `IMAGE_POOL` を追加

## 5. 実績・声

- 利用者の声・実績件数は **確認できないため未掲載**（架空の口コミは作らない方針）
- 実際の声が集まったら `Testimonials` セクションを TOP に追加（本人の許諾必須）
- ケアマネジャー等との連携実績は **未確認のため「連携しています」とは書いていない**

## 6. 公開前の設定

- [ ] `NEXT_PUBLIC_SITE_URL` に本番ドメインを設定（Vercel 環境変数）
- [ ] GitHub Secrets `ANTHROPIC_API_KEY` を設定（ブログ自動投稿）
- [ ] Google Search Console にサイトマップ（`/sitemap.xml`）を登録
- [ ] Googleビジネスプロフィールの作成・NAP統一
- [ ] Instagram プロフィールにサイトURLを設置
